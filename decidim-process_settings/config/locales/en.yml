---
en:
  decidim:
    components:
      process_settings:
        name: Extra process settings
        settings:
          global:
            automatic_step_change: Change the active phase automatically based on
              the current date
    process_settings:
      admin:
        components:
          creation_not_allowed: This component can only be added once, and only to
            a participatory process.
        settings:
          show:
            explanation: This component only provides extra settings to administer
              the participatory process.
            no_front: It has no public (front-end) part.
        steps:
          modal:
            active: Active phase
            apply: Apply
            gap_html: 'Between the end of %{after} and the start of %{before}: %{from}
              - %{to}'
            gap_note: Time periods between the first and the last date of all phases
              that are not covered by any phase. In those cases the automatic change
              keeps the previous phase active until the next one begins.
            gaps_title: Gaps between phases
            no_steps: This process has no phases yet.
            none_now: No phase covers the current date.
            ovelap_note: When there are phases whose dates overlap, the first one
              by position is automatically activated while they overlap.
            overlap_html: 'Overlap of %{a} and %{b}: %{from} - %{to}'
            overlaps_title: Overlapping phases
            status:
              current: Matches the current date
              current_will_activate: Matches the current date, it will be activated
                automatically if the automatic phase change is enabled
              kept_active: Currently active — no phase covers the current date,
                so the automatic change keeps it active
              overlapped: Overlaps now with an earlier phase — the earlier one (by
                position) is the one activated
              past: Ended
              undated: No dates — it is never activated automatically
              upcoming: Starts %{start}
            status_disabled: Automatic phase change is disabled.
            status_enabled: Automatic phase change is enabled.
            summary_title: Phases schedule
            switch_label: Change the active phase automatically based on the current
              date
            title: Automatic phase change
          toggle:
            button: Automatic phase change
            disabled: Disabled
            enabled: Enabled
