<%= hidden_field_tag "newsletter[send_to_selected_users]", params.dig(:newsletter, :send_to_selected_users) %>
<% @form.selected_users_ids.each do |user_id| %>
  <%= hidden_field_tag "newsletter[selected_users_ids][]", user_id, id: "selected_users_ids_#{user_id}" %>
<% end %>
